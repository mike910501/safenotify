require('dotenv').config(); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function checkAllTemplates() { console.log('🔍 REVISANDO TODAS LAS PLANTILLAS'); try { const allTemplates = await prisma.template.findMany({ orderBy: { updatedAt: 'desc' } }); console.log('Total plantillas:', allTemplates.length); allTemplates.forEach((template, index) => { console.log(); console.log(); console.log(); console.log('   ---'); }); } catch (error) { console.error('Error:', error); } finally { await prisma.(); } } checkAllTemplates().catch(console.error);
